name: Laravel cloudflare static analysis

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  statics:
    name: Static analysis
    uses: monicahq/workflows/.github/workflows/static.yml@20240504-static
    with:
      php-version: 8.3
